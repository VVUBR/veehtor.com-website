Update the `WHATSAPP_URL` constant in `src/pages/Cervejarias.tsx` from the current value (which includes a pre-filled message) to the exact URL provided by the user: `https://api.whatsapp.com/send?phone=5511973022058`.

This single-line change will automatically update every "Falar no WhatsApp" button and the floating WhatsApp FAB across the entire `/cervejarias` page, since they all reference the same constant.